spring:
  port: 8080
  application:
    name: greeting-service

greeting-service:
  greeting: "Hello"
  defaultValue: "World"

management:
  endpoints:
    web:
      exposure:
        include:
          - '*'
  endpoint:
    health:
      show-details: ALWAYS